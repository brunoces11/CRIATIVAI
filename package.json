{
  "name": "criativai-landing-page",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "backend": "python -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000",
    "backend:migrate": "python -m alembic -c backend/alembic.ini upgrade head",
    "start": "npx serve@latest dist",
    "test:backend": "node tests/backend-tests.mjs",
    "test": "npm run build && node --test tests/rendered-html.test.mjs",
    "test:vertical": "npm run build && node --test tests/backend-vertical.test.mjs",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.2",
    "typescript": "5.9.3",
    "vite": "^8.1.5"
  },
  "type": "module"
}
